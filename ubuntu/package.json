{
  "name": "ubuntu",
  "private": "true",
  "scripts": {
    "watch": "nodemon --watch Dockerfile --exec \"pnpm build\"",
    "build": "docker compose build --progress=plain",
    "start": "docker compose down && docker compose up -d && docker compose exec ubuntu bash"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
