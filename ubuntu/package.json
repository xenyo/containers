{
  "name": "ubuntu",
  "private": "true",
  "scripts": {
    "watch": "nodemon --watch Dockerfile --exec \"pnpm build\"",
    "build": "docker compose build --progress=plain",
    "start": "docker compose run --rm ubuntu bash"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
