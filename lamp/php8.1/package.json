{
  "name": "lamp-php8.1",
  "private": "true",
  "scripts": {
    "watch": "nodemon --watch Dockerfile --exec \"npm run build && npm run push\"",
    "build": "docker build -t xenyo/lamp:php8.1 -t xenyo/lamp --progress=plain .",
    "push": "docker push xenyo/lamp:php8.1 && docker push xenyo/lamp"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
