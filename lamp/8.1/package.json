{
  "name": "lamp-8.1",
  "private": "true",
  "scripts": {
    "watch": "nodemon --watch Dockerfile --exec \"pnpm build\"",
    "build": "docker build -t xenyo/lamp:8.1 -t xenyo/lamp --progress=plain .",
    "push": "docker push xenyo/lamp:8.1 && docker push xenyo/lamp",
    "start": "docker compose up -d && docker compose exec lamp bash",
    "clean": "docker compose down -v"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
